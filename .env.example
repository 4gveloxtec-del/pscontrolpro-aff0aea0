# =============================================================================
# PSControl Pro - Environment Variables Template
# =============================================================================
# 
# Copy this file to .env and fill in the values.
# All environment variables MUST start with VITE_ to be accessible in the browser.
#
# IMPORTANT: Never commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Get these values from your Supabase project settings or Lovable Cloud:
# Lovable Cloud: Settings → Backend → Connection Info
# Supabase: https://supabase.com/dashboard/project/_/settings/api

# Your Supabase project URL (e.g., https://abc123.supabase.co)
VITE_SUPABASE_URL=

# Your Supabase anon/public key (safe to expose in browser)
# Lovable uses VITE_SUPABASE_PUBLISHABLE_KEY, standard Supabase uses VITE_SUPABASE_ANON_KEY
# Both are supported - use whichever matches your setup
VITE_SUPABASE_PUBLISHABLE_KEY=

# Your Supabase project ID (the 'abc123' part of the URL above)
VITE_SUPABASE_PROJECT_ID=

# -----------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# -----------------------------------------------------------------------------

# Enable debug mode for development (set to "true" to enable)
# VITE_DEBUG_MODE=false

# =============================================================================
# DEPLOYMENT CHECKLIST
# =============================================================================
#
# For VERCEL deployment:
# 1. Go to Vercel Dashboard → Project → Settings → Environment Variables
# 2. Add ALL required variables above (VITE_SUPABASE_URL, VITE_SUPABASE_PUBLISHABLE_KEY, VITE_SUPABASE_PROJECT_ID)
# 3. Make sure to enable for Production, Preview, AND Development environments
# 4. Redeploy after adding/changing variables
#
# Build Settings for Vercel:
# - Framework Preset: Vite
# - Build Command: npm run build
# - Output Directory: dist
# - Install Command: npm install
#
# For LOCAL development:
# 1. Copy this file to .env
# 2. Fill in the values from your Supabase/Lovable Cloud project
# 3. Run: npm run dev
#
# =============================================================================
# BACKEND SECRETS (Edge Functions)
# =============================================================================
# These are configured in Supabase/Lovable Cloud dashboard, NOT in .env file:
# - SUPABASE_URL (auto-injected)
# - SUPABASE_ANON_KEY (auto-injected)
# - SUPABASE_SERVICE_ROLE_KEY (auto-injected)
# - ENCRYPTION_KEY (for data encryption)
# - VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT (for push notifications)
# - LOVABLE_API_KEY (for AI features)
#
# =============================================================================
